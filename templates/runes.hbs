<div class="addendum" id="sundry-property-runes">
  <h4>
    {{localize "sundry.display.property-runes"}}
  </h4>
  {{#each runes}}
  <strong>
    {{{name}}}
  </strong>
  @Embed[{{uuid}} inline]
  {{/each}}
</div>